# for one device, can be given like this:
hosts:
  - device: 1.1.1.1
    username : abc
    passwd: xyz
  - device: 1.1.1.15
    username : abc
    passwd: xyz
  - device: 1.1.1.16
    username : abc
    passwd: xyz
tests:
  - delta.yml
sqlite:
  - store_in_sqlite: no
    check_from_sqlite: yes
    database_name: jbb.db
    compare: 0,1

