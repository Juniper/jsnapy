# for one device, can be given like this:
hosts:
  - device: 10.216.193.114
    username : abc
    passwd: xyz
tests:
  - test_incorrect_2.yml
sqlite:
  - store_in_sqlite: no
    check_from_sqlite: yes
    database_name: jbb.db

