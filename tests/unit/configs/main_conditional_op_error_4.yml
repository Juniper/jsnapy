
# for one device, can be given like this:
hosts:
  - device: 1.1.1.1
    username : abc
    passwd: xyz
tests:
  - conditional_op_error_4.yml

sqlite:
  - store_in_sqlite: yes
    check_from_sqlite: no
    database_name: jbb.db

