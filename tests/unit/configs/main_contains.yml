
# for one device, can be given like this:
hosts:
  - devices: 10.216.193.114 
    username : root 
    passwd: Embe1mpls
tests:
  - contains.yml

sqlite:
  - store_in_sqlite: yes
    check_from_sqlite: no
    database_name: jbb.db

