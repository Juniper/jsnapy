
# for one device, can be given like this:
hosts:
  - devices: 10.21.13.4 
    username : demo 
    passwd: demo123
tests:
  - contains.yml

sqlite:
  - store_in_sqlite: yes
    check_from_sqlite: no
    database_name: jbb.db

